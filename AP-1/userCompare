public int userCompare(String aName, int aId, String bName, int bId) {
  if (aName.compareTo(bName) < 0) return -1;
  return aName.equals(bName) && aId == bId? 0 : aName.equals(bName) && aId < bId? -1 : 1;
}
