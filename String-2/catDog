public boolean catDog(String str) {
  int var = 0, vare = 0;
  for(int i = 0;i<str.length()-2;i++)
    if(str.substring(i,i+3).equals("cat")) var++;
  for(int i = 0;i<str.length()-2;i++)
    if(str.substring(i,i+3).equals("dog")) vare++;
  return var == vare;
}
