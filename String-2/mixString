public String mixString(String a, String b) {
  String res = "";
  for (int i = 0; i < Math.min(a.length(), b.length()); i++) res += "" + a.charAt(i) + b.charAt(i);
  return a.length() > b.length()? res + a.substring(b.length()) : res + b.substring(a.length());
}
