public int[] pre4(int[] nums) {
  int c = 0;
  for (int i = 0; i < nums.length; i++) {
    if (nums[i] != 4) c++;
    else break;
  }
  int[] r = new int[c];
  for (int i = 0; i < r.length; i++) r[i] = nums[i];
  return r;
}
