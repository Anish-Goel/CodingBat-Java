public int[] evenOdd(int[] nums) {
  int[] r = new int[nums.length];
  int e = 0, o = r.length - 1;
  for (int i = 0; i < r.length; i++)
    if (nums[i] % 2 == 0) r[e++] = nums[i];
    else r[o--]  = nums[i];
  return r;
}
