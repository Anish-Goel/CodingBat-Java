public boolean either24(int[] nums) {
    boolean t = false, f = false;
    for (int i = 0; i < nums.length - 1; i++) {
      if (nums[i] == 2 && nums[i + 1] == 2) t = true;
      if (nums[i] == 4 && nums[i + 1] == 4) f = true;
    }
    return t ^ f;
}
